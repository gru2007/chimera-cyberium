# vim:set ft=sh

MODULES=(dm_mod ext4 sha256 sha512 overlay apple-bce)
BINARIES=()
FILES=()
HOOKS=(microcode systemd modconf kms keyboard keymap sd-vconsole block filesystems fsck)
COMPRESSION="xz"
COMPRESSION_OPTIONS=(-v -9e)
